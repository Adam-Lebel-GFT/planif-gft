/* ════════════════════════════════════════════════════════════
   Poker Planning — connexion temps réel (mode Live)

   Renseigne ici tes deux valeurs Supabase, une fois pour toutes
   (Supabase → ton projet → Settings → API) :
     url : « Project URL »   ex. 'https://abcdefgh.supabase.co'
     key : clé « anon public »  ex. 'eyJhbGciOiJIUzI1NiIs…'

   La clé anon est PUBLIQUE par design — aucun secret ici.
   Une fois rempli : le bloc de config disparaît du lobby et les
   liens d'invitation deviennent courts (…?room=PK-XXXX).
   Tant que c'est vide, l'outil retombe sur la saisie manuelle.
   ════════════════════════════════════════════════════════════ */
window.PK_CONFIG = {
  url: '',
  key: ''
};
